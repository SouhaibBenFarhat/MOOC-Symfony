{% extends "EspritMoocBundle::Layout.html.twig" %}

{% block content %}
    <br>
    <br>
    <br>
    <br>
    <br>
    <section id="portfolio">
        <div class="container" id="wrap">
            <div class="row">

                <fieldset class="scheduler-border">
                    <div class="col-md-6 col-md-offset-3">

                        {# {% for app in apprenant %}
                             {{ app.username }}
                         {% endfor %}#}

                        <form action="{{ path('sauvegarder_quizz_cours') }}" method="post" class="form"><legend><b> Ajouter Quizz Cours1</b></legend>.

                            {%  for i in 1..nombreQuestion %}
                                <br><br>
                                <legend><b>  Question {{ i }}</b></legend><br>
                                <input type="text" class="form-control input-lg" name="question{{i}}"><br>
                                {%  for j in 1..nombreReponse %}
                                    Reponse {{ j }}<br>
                                    <input type="text" class="form-control"  name="reponse{{i}}{{j}}" value="">
                                    <input type="checkbox" id="remember_me" name="etatreponse{{i}}{{j}}" value="Vrai" class="" /> <label for="remember_me" class="checkbox-inline"><b> </b></label><br><br>

                                {% endfor %}
                                <div class="alert alert-info"><center> ce texte n'apparait que lors de l'affichage de la correction</center></div>
                                <textarea name="explication{{i}}" class="form-control input-lg" placeholder="Expliquer Brievement votre question"></textarea><br>
                            {% endfor %}
                            <input type="text" class=""  name="nombreQuestion" value="{{ nombreQuestion }}" hidden >
                            <input type="text" class=""  name="nombreReponse" value="{{ nombreReponse }}" hidden >
                            <input type="text" class=""  name="idQuizzCours" value="{{ idQuizzCours }}"  hidden>
                            <div>
                                
                                <input type="submit" value="Suivant" class='btn-primary' />
                            </div>


                        </form>

                    </div>
                </fieldset>
            </div>            
        </div>
    </section>
{% endblock content %}